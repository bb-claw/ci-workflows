# ─────────────────────────────────────────────────────────────────────────────
# deploy-railway.yml
#
# Triggers a Railway service redeployment. Railway pulls the latest image
# from ghcr.io (the same digest built by docker-build-push.yml).
#
# Prerequisites:
#   - Railway service must be configured to use a Docker image source
#     pointing to ghcr.io/bb-claw/<service-name>
#   - RAILWAY_TOKEN secret must be scoped to the correct project + environment
#
# Inputs:
#   service-id    — Railway service ID (from Railway dashboard → service settings)
#   environment   — GitHub environment name ("dev" or "production")
# ─────────────────────────────────────────────────────────────────────────────
name: Deploy to Railway

on:
  workflow_call:
    inputs:
      service-id:
        description: "Railway service ID"
        type: string
        required: true
      project-id:
        description: "Railway project ID (optional, may be required for some tokens)"
        type: string
        required: false
      environment-id:
        description: "Railway environment ID (optional, may be required for some tokens)"
        type: string
        required: false
      environment:
        description: "GitHub environment name (dev | production)"
        type: string
        required: true
    secrets:
      RAILWAY_TOKEN:
        required: true

jobs:
  deploy:
    name: Deploy → Railway (${{ inputs.environment }})
    runs-on: ubuntu-latest
    environment:
      name: ${{ inputs.environment }}

    steps:
      - name: Install Railway CLI
        uses: bb-claw/ci-workflows/.github/actions/setup-railway@v1

      - name: Debug Railway Connection
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          echo "🔍 Debugging Railway connection..."
          echo ""
          echo "1️⃣ Checking token validity..."
          railway whoami || echo "⚠️ whoami failed"
          echo ""
          echo "2️⃣ Listing available services..."
          railway service list || echo "⚠️ service list failed"
          echo ""
          echo "3️⃣ Service ID to deploy: ${{ inputs.service-id }}"
          echo "4️⃣ Project ID: ${{ inputs.project-id || '(not provided)' }}"
          echo "5️⃣ Environment ID: ${{ inputs.environment-id || '(not provided)' }}"

      - name: Trigger redeployment
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          set -e

          echo "🚀 Triggering redeployment for service: ${{ inputs.service-id }}"
          echo ""

          SERVICE_ID="${{ inputs.service-id }}"
          PROJECT_ID="${{ inputs.project-id }}"
          ENVIRONMENT_ID="${{ inputs.environment-id }}"

          # Step 1: If project ID provided, select it
          if [[ -n "$PROJECT_ID" ]]; then
            echo "📍 Selecting project: $PROJECT_ID"
            railway project select "$PROJECT_ID" || {
              echo "❌ Failed to select project"
              exit 1
            }
          fi

          # Step 2: If environment ID provided, select it
          if [[ -n "$ENVIRONMENT_ID" ]]; then
            echo "📍 Selecting environment: $ENVIRONMENT_ID"
            railway environment select "$ENVIRONMENT_ID" || {
              echo "❌ Failed to select environment"
              exit 1
            }
          fi

          # Step 3: Trigger redeploy with only service ID and yes flag
          echo "▶️ Executing: railway redeploy --service $SERVICE_ID --yes"
          railway redeploy --service "$SERVICE_ID" --yes || {
            echo ""
            echo "❌ Redeploy command failed"
            echo ""
            echo "Debugging: Checking if service exists..."
            railway service list --json | jq '.[] | select(.id == "'$SERVICE_ID'")'
            exit 1
          }

          echo ""
          echo "✅ Redeploy command executed successfully"

      - name: Wait for deployment
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          echo "⏳ Waiting for deployment to start..."

          SERVICE_ID="${{ inputs.service-id }}"
          MAX_WAIT=30
          POLL_INTERVAL=2

          for i in $(seq 1 $((MAX_WAIT / POLL_INTERVAL))); do
            STATUS=$(railway service list --json 2>/dev/null | \
              jq -r ".[] | select(.id == \"$SERVICE_ID\") | .status" 2>/dev/null || echo "unknown")

            echo "[$i/$((MAX_WAIT / POLL_INTERVAL))] Status: $STATUS"

            if [[ "$STATUS" == "BUILDING" || "$STATUS" == "DEPLOYING" || "$STATUS" == "CRASHED" ]]; then
              echo "✅ Deployment detected — service is $STATUS"
              exit 0
            fi

            sleep $POLL_INTERVAL
          done

          echo "⚠️ Could not detect deployment status (may be queued)"

      - name: Summary
        run: |
          {
            echo "## Railway Deployment"
            echo "| | |"
            echo "|---|---|"
            echo "| **Environment** | \`${{ inputs.environment }}\` |"
            echo "| **Service ID** | \`${{ inputs.service-id }}\` |"
            if [[ -n "${{ inputs.project-id }}" ]]; then
              echo "| **Project ID** | \`${{ inputs.project-id }}\` |"
            fi
            if [[ -n "${{ inputs.environment-id }}" ]]; then
              echo "| **Environment ID** | \`${{ inputs.environment-id }}\` |"
            fi
            echo "| **Status** | ✅ Redeployment triggered |"
          } >> "$GITHUB_STEP_SUMMARY"
